var root = document.querySelector("#root");

// Tao element
var h1 = document.createElement("h1");

h1.innerText = `Hoc JS khong kho`;
h1.id = "title";
h1.className = "title";
// console.log(h1);

root.append(h1);
